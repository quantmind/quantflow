#!/usr/bin/env bash
set -e

BLACK_ARG="--check"
RUFF_ARG=""

if [ "$1" = "fix" ] ; then
    BLACK_ARG=""
    RUFF_ARG="--fix"
fi

black quantflow tests ${BLACK_ARG}
ruff quantflow tests ${RUFF_ARG}
mypy quantflow
